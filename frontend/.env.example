# Public site configuration
VITE_API_BASE_URL=http://localhost:3000/api
VITE_APP_NAME=Enderas Asset Management

# Mock admin auth (frontend-only; replace with JWT when backend is ready)
VITE_ADMIN_PASSWORD=enderas-admin

# When backend is ready, swap mock services in src/services/ to use VITE_API_BASE_URL.
# Components and pages consume services only — no code changes required in UI layer.
