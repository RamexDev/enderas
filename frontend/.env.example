# Public site configuration
VITE_API_BASE_URL=http://localhost:3000/api/v1
VITE_APP_NAME=Enderas Asset Management

# Mock admin auth (legacy in-app admin only; production CMS is the separate admin app)
VITE_ADMIN_PASSWORD=enderas-admin
